float calculo_a (float preco) // importando valores de main e declarando variáveis;
{
    int res;

    res = preco - (preco * 50) / 100;

    return res; // retornando com o valor do calculo.
}

float calculo_b (float preco)
{
    int res;

    res = preco - (preco * 25) / 100;

    return res; // retornando com o valor do calculo.
}

float calculo_c(float preco)
{
    int res;

    res = preco - (preco * 15) / 100;

    return res; // retornando com o valor do calculo.
}

float calculo_d(float preco)
{
    int res;

    res = preco - (preco * 5) / 100;

    return res; // retornando com o valor do calculo.
}

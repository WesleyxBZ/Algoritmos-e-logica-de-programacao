int calculo(int salario) // importando valores do main e declarando-os;
{
    int res;

    res = 5 * salario; // calculnado;

    return res; // returnando ao main, ou função chamadora, com o resultado do cálculo.
}
